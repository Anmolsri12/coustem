import 'package:flutter/material.dart';


class Anmol extends StatelessWidget {
  const Anmol({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    padding:const EdgeInsets.all(12.0),
    child:const Text("My name Anmol Srivastava"),);
  }
}